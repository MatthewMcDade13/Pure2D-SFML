#pragma once
#include "declspec.h"

namespace pure
{
	// Converts degrees to radians
	float PUREENGINE_API radians(float deg);

	// Converts radians to degrees
	float PUREENGINE_API degress(float radians);
}